## Cursor Cloud specific instructions

This is a Python project (named "novel"). The `.gitignore` is configured for Python.

### Current state

As of initial setup, the repository contains no application code, no dependency files (`requirements.txt`, `pyproject.toml`, etc.), and no services. Future agents should check whether source code or dependency files have been added since this note was written.

### Environment

- **Python**: 3.12.3 (system, at `/usr/bin/python3`)
- **pip**: available (`pip3`)
- No virtual environment is pre-created; create one with `python3 -m venv .venv` if dependencies are added.

### Running / Testing

No application or tests exist yet. Once code is added, update this section with lint, test, and run commands.
